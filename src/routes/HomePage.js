import { Navigation } from '../components/Navigation'
import Body from '../components/Body'

export const HomePage = () => { 
    return (
        <div>
            <Navigation/>
            <Body/>
        </div>
    );
}
